; Render (and other hosts) set NODE_ENV=production, which makes npm omit
; devDependencies. Our build needs them (typescript, vite, @types/node for the
; Convex typecheck), so force-include the dev group on every install.
include=dev
